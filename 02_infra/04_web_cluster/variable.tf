variable "web_port" {
  description = "the port will use for HTTP requests"
  type = number
  default = 8080
}

variable "ssh_port" {
  description = "the port will use for HTTP requests"
  type = number
  default = 22
}